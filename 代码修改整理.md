# 代码修改整理

## 当前工作区修改分类汇总

---

### 一、核心需求：从缺陷跟踪模块移除高级搜索中"所属工作空间"和"所属项目"筛选条件

**需求说明**：缺陷跟踪模块（缺陷管理页面、测试计划列表页面）用户已经在特定项目内，不需要跨项目筛选；**工作台模块（独立JQL综合查询）保留**跨工作空间/跨项目筛选功能。

| 模块 | 处理方式 | 文件 | 修改内容 |
|---------|---------|---------|---------|
| ✅ **缺陷管理 - IssueList** | **移除** | `test-track/frontend/src/business/issue/IssueList.vue` | 删除 `hasCrossProjectFilter()` 方法，删除查询和导出中清除 `projectId/workspaceId` 的逻辑 |
| ✅ **测试计划列表 - TestPlanList** | **移除** | `test-track/frontend/src/business/plan/components/TestPlanList.vue` | 删除 `hasCrossProjectFilter()` 方法，删除导出中清除 `projectId/workspaceId` 的逻辑，删除重复的 `selectAll` 参数 |
| ✅ **缺陷管理 SQL - ExtIssuesMapper** | **移除** | `test-track/backend/src/main/java/io/metersphere/base/mapper/ext/ExtIssuesMapper.xml` | 删除 `workspaceIds` 和 `projectIds` 两个SQL条件 |
| ✅ **工作台 - AdvancedSearch** | **保留** | `workstation/frontend/src/business/advanced-search/AdvancedSearch.vue` | 已恢复工作空间/项目选择UI和方法 |
| ✅ **工作台 - TopFilterBar** | **保留** | `workstation/frontend/src/business/advanced-search/components/TopFilterBar.vue` | 已恢复工作空间/项目选择UI、数据、方法、样式 |
| ✅ **工作台 - Store** | **保留** | `workstation/frontend/src/store/advancedSearch.js` | 已恢复状态定义、相关方法、参数传递 |
| ✅ **工作台 - Request DTO** | **保留** | `workstation/backend/src/main/java/io/metersphere/request/AdvancedSearchRequest.java` | 已恢复 `workspaceIds` / `projectIds` 字段 |
| ✅ **工作台 - Controller** | **保留** | `workstation/backend/src/main/java/io/metersphere/workstation/controller/AdvancedSearchController.java` | 已恢复 `getWorkspaces` / `getProjects` 接口 |
| ✅ **工作台 - Mapper** | **保留** | `workstation/backend/src/main/java/io/metersphere/base/mapper/ext/ExtAdvancedSearchMapper.xml` | 已恢复 `workspaceProjectFilter` SQL片段和四个查询中的include |

---

### 二、代码质量优化（simplify 代码审查后修复）

| 文件 | 修复内容 |
|---------|---------|
| `api-test/frontend/src/business/automation/scenario/ApiScenarioList.vue` | 删除自定义防抖实现，改用项目已有的 `lodash-es/debounce`，改进beforeDestroy内存清理 |
| `api-test/frontend/src/business/automation/scenario/EditApiScenario.vue` | 改进并行加载 - 使用 `Promise.all` 添加错误处理，所有请求全部并行，移除不必要的 `$nextTick` 包装，减少首屏加载时间 |
| `test-track/backend/src/main/java/io/metersphere/plan/service/TestPlanService.java` | 抽取私有辅助方法 `parseCountValue()`，消除5处重复的计数解析代码 |
| `workstation/frontend/src/api/` | 恢复之前误删的整个API目录，只保留修改后的 `advanced-search.js`，修复编译错误 |

---

### 三、新增功能：测试计划导出

| 文件 | 修改类型 |
|---------|---------|
| `test-track/backend/src/main/java/io/metersphere/plan/request/TestPlanExportRequest.java` | **新增** - 测试计划导出请求类 |
| `test-track/backend/src/main/java/io/metersphere/plan/request/QueryTestPlanRequest.java` | 重构继承结构，改为继承 `BaseQueryRequest`，减少重复代码 |
| `test-track/backend/src/main/java/io/metersphere/controller/TestPlanController.java` | 添加导出接口 |
| `test-track/backend/src/main/java/io/metersphere/plan/service/TestPlanService.java` | 实现 `testPlanExport()` 导出方法 |
| `test-track/frontend/src/business/plan/components/TestPlanList.vue` | 添加导出按钮和导出处理 |
| `test-track/frontend/src/business/plan/components/export/` | **新增** - 导出组件目录 |
| `test-track/frontend/src/i18n/lang/en-US.js` / `zh-CN.js` / `zh-TW.js` | 添加导出功能国际化（三种语言） |

---

### 四、缺陷跟踪模块改进

| 文件 | 修改内容 |
|---------|---------|
| `test-track/backend/src/main/java/io/metersphere/service/IssueStatusTransitionService.java` | 修复：任意状态流转到 `reopened` 都触发动作，不只是 closed → reopened |
| `test-track/backend/src/main/java/io/metersphere/service/IssuesService.java` | 添加 `userId` 权限过滤，改进跨项目搜索安全性 |
| `test-track/backend/src/main/java/io/metersphere/base/mapper/ext/ExtIssuesMapper.xml` | 添加跨项目搜索时的用户权限过滤SQL |

---

### 五、接口测试页面性能优化

| 文件 | 优化内容 |
|---------|---------|
| `api-test/frontend/src/business/automation/scenario/ApiScenarioList.vue` | 添加防抖，减少项目节点选择变化时重复搜索请求，降低服务器压力 |
| `api-test/frontend/src/business/automation/scenario/EditApiScenario.vue` | 关键数据并行加载，非关键数据不阻塞首屏，减少总加载时间 |

---

### 六、其他修改

| 分类 | 文件 |
|---------|---------|
| 需求池功能 | `test-track/backend/src/main/java/io/metersphere/requirement/pool/controller/RequirementPoolController.java` - 添加普通搜索支持 |
| 基础服务 | `framework/sdk-parent/sdk/src/main/java/io/metersphere/service/BaseUserService.java` - 小功能修改 |
| 文档清理 | 删除4份旧文档文件（需求统计、评估报告等） |

---

### 处理范围总结

| 模块 | 所属工作空间/项目筛选 |
|---------|---------|
| **工作台独立JQL综合查询** | ✅ 保留 - 需要跨项目查询功能 |
| **缺陷跟踪 - 缺陷管理页面高级搜索** | ✅ 移除 - 用户已在特定项目内 |
| **缺陷跟踪 - 测试计划列表高级搜索** | ✅ 移除 - 用户已在特定项目内 |

---

### 统计

- **修改文件**：45个
- **新增文件**：测试计划导出相关组件 + 规格文档目录
- **删除文件**：4份旧文档

---

*生成时间：2026-03-31*
