{
  "enabled": true,
  "name": "任务完成后经验评估",
  "description": "在每次 agent 执行完成后，自动评估是否有值得记录的经验教训，并按格式追加到 .learnings/ 对应文件中",
  "version": "1",
  "when": {
    "type": "agentStop"
  },
  "then": {
    "type": "askAgent",
    "prompt": "回顾刚才的任务执行过程，评估是否有值得记录的经验：\n1. 是否发现了非显而易见的解决方案？\n2. 是否有命令/操作失败需要记录？\n3. 是否纠正了之前的错误认知？\n4. 是否发现了项目特有的模式或约定？\n5. 是否有更好的做法值得记录？\n\n如果有，按照 .kiro/steering/self-improvement.md 中的格式追加到对应的 .learnings/ 文件中。如果没有值得记录的内容，不需要做任何操作。"
  }
}