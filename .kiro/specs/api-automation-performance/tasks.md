# 任务清单：接口测试前端性能优化

> 范围：`api-test/frontend/src/business/automation/`
> 策略：先建立基线，再按“版本对比虚拟化 -> 刷新链路 -> 深拷贝 -> 单组件 -> 按需挂载”顺序推进。

---

## 阶段 0：性能基线与样本准备

### 0.1 大场景样本

- [ ] 0.1.1 准备 20 步场景样本，用于小场景兼容验证
- [ ] 0.1.2 准备 50 步场景样本，用于虚拟滚动阈值边界验证
- [ ] 0.1.3 准备 100 步场景样本，用于常见大场景验证
- [ ] 0.1.4 准备 200 步场景样本，用于极限压力验证
- [ ] 0.1.5 样本需覆盖 HTTP 请求、场景引用、循环、事务、If 控制器、断言、脚本组件

### 0.2 指标记录

- [ ] 0.2.1 记录普通编辑器打开耗时、DOM 数量、JS heap 峰值
- [ ] 0.2.2 记录最大化编辑器打开耗时、DOM 数量、JS heap 峰值
- [ ] 0.2.3 记录版本对比页打开耗时、DOM 数量、JS heap 峰值
- [ ] 0.2.4 记录保存按钮点击到请求发出耗时
- [ ] 0.2.5 记录调试按钮点击到请求发出耗时
- [ ] 0.2.6 记录 Chrome Performance 主线程长任务

---

## 阶段 1：版本对比页左右步骤树虚拟化

### 1.1 代码梳理

- [x] 1.1.1 梳理 `ScenarioDiff.vue` 左侧旧版本步骤树模板
- [x] 1.1.2 梳理 `ScenarioDiff.vue` 右侧新版本步骤树模板
- [x] 1.1.3 梳理 `nodeExpand`、`nodeCollapse`、`nodeClick`、`oldNodeClick` 参数差异
- [x] 1.1.4 梳理 `diff(oldVnode, vnode, oldColor, newColor)` 对 DOM 的依赖

### 1.2 虚拟树实现

- [x] 1.2.1 增加 `countVisibleStepNodes(nodes, parent)` 递归统计方法
- [x] 1.2.2 增加 `useOldVirtualStepTree` 计算属性
- [x] 1.2.3 增加 `useNewVirtualStepTree` 计算属性
- [x] 1.2.4 左侧步骤树超过阈值时切换为 `vue-virtual-tree`
- [x] 1.2.5 右侧步骤树超过阈值时切换为 `vue-virtual-tree`
- [x] 1.2.6 小场景保留原 `el-tree`
- [ ] 1.2.7 抽取左右树公共节点渲染逻辑，减少模板重复

### 1.3 功能验证

- [ ] 1.3.1 验证小场景版本对比页行为不变
- [ ] 1.3.2 验证大场景左右树滚动正常
- [ ] 1.3.3 验证展开、收起状态正常
- [ ] 1.3.4 验证点击左右节点后 `ScenarioChildDiff` 正常打开
- [ ] 1.3.5 验证 diff 高亮无阻塞性异常
- [ ] 1.3.6 验证 DOM 数量相比 `el-tree` 明显下降

---

## 阶段 2：强制刷新链路重构

### 2.1 调用点审计

- [x] 2.1.1 查找所有 `forceRerenderIndex` 读写点
- [x] 2.1.2 查找所有通过 `getUUID()` 触发 UI 刷新的逻辑
- [x] 2.1.3 查找所有临时 `push + splice` 刷新树的逻辑
- [x] 2.1.4 按刷新目的分类：序号、勾选、展开、选中、隐藏节点

### 2.2 局部刷新设计

- [x] 2.2.1 为步骤序号刷新设计局部响应式方案
- [x] 2.2.2 为批量处理状态刷新设计树状态版本号或公开 API 调用
- [x] 2.2.3 为新增断言/步骤后的父节点刷新设计局部更新方案
- [x] 2.2.4 移除 `MaximizeScenario.vue` 中的临时 `push + splice`
- [x] 2.2.5 移除或收敛 `Menu.js` 中对全局刷新 key 的直接写入

### 2.3 回归验证

- [ ] 2.3.1 验证新增 HTTP 请求步骤
- [ ] 2.3.2 验证新增断言步骤
- [ ] 2.3.3 验证新增控制器步骤
- [ ] 2.3.4 验证普通编辑器批量处理
- [ ] 2.3.5 验证最大化编辑器批量处理
- [ ] 2.3.6 验证虚拟树模式下刷新行为稳定

---

## 阶段 3：保存/调试大对象深拷贝结构化优化

### 3.1 深拷贝审计

- [x] 3.1.1 查找 `JSON.parse(JSON.stringify())` 使用点
- [x] 3.1.2 查找保存链路中的全树递归和复制逻辑
- [x] 3.1.3 查找调试链路中的全树递归和复制逻辑
- [x] 3.1.4 查找版本对比链路中的复制和字段清理逻辑

### 3.2 payload 构造

- [x] 3.2.1 设计保存 payload 字段白名单
- [x] 3.2.2 设计调试 payload 字段白名单
- [x] 3.2.3 设计版本 diff payload 字段规范化规则
- [x] 3.2.4 新增 `buildScenarioSavePayload`
- [x] 3.2.5 新增 `buildScenarioDebugPayload`
- [x] 3.2.6 新增 `buildScenarioDiffPayload`

### 3.3 正确性验证

- [ ] 3.3.1 验证普通场景保存结果一致
- [ ] 3.3.2 验证带文件上传场景保存结果一致
- [ ] 3.3.3 验证带场景引用场景保存结果一致
- [ ] 3.3.4 验证调试运行结构正确
- [ ] 3.3.5 验证版本对比结果正确
- [ ] 3.3.6 对比保存/调试主线程耗时和内存峰值

---

## 阶段 4：单步骤组件内部渲染成本优化

### 4.1 组件识别

- [ ] 4.1.1 用 Performance 识别展开耗时最高的步骤组件
- [ ] 4.1.2 标记复杂断言组件的重渲染来源
- [ ] 4.1.3 标记脚本编辑器组件初始化成本
- [ ] 4.1.4 标记请求体/参数表/文件列表组件的大列表成本

### 4.2 组件优化

- [ ] 4.2.1 复杂断言列表使用稳定 key
- [ ] 4.2.2 DOM 查询改为组件内 ref
- [ ] 4.2.3 DOM 测量使用 RAF 并在销毁时取消
- [ ] 4.2.4 脚本编辑器折叠态不挂载实例
- [ ] 4.2.5 请求体和参数表减少无意义 deep watch
- [ ] 4.2.6 大列表拆分行组件，降低父组件重渲染成本

### 4.3 回归验证

- [ ] 4.3.1 验证断言增删改查
- [ ] 4.3.2 验证脚本编辑器展开、编辑、保存
- [ ] 4.3.3 验证请求体参数编辑
- [ ] 4.3.4 验证文件上传字段保存
- [ ] 4.3.5 验证折叠/展开不会丢失表单数据

---

## 阶段 5：大场景按需展开与按需挂载

### 5.1 状态协议

- [ ] 5.1.1 设计节点折叠态只渲染标题行的协议
- [ ] 5.1.2 设计展开态挂载直接子步骤的协议
- [ ] 5.1.3 设计编辑态挂载完整步骤组件的协议
- [ ] 5.1.4 设计批量展开超过阈值时的保护策略

### 5.2 实现

- [ ] 5.2.1 `ComponentConfig.vue` 支持按可见/展开状态挂载重组件
- [ ] 5.2.2 容器节点支持子步骤按需挂载
- [ ] 5.2.3 虚拟树模式下保持选中和展开状态
- [ ] 5.2.4 批量展开时避免一次性挂载全部重组件

### 5.3 回归验证

- [ ] 5.3.1 验证场景引用节点
- [ ] 5.3.2 验证循环控制器节点
- [ ] 5.3.3 验证事务控制器节点
- [ ] 5.3.4 验证 If 控制器节点
- [ ] 5.3.5 验证拖拽排序后展开状态
- [ ] 5.3.6 验证未挂载子组件的数据保存不丢失

---

## 阶段 6：构建、性能验证与发布

### 6.1 自动验证

- [ ] 6.1.1 执行 `git diff --check`
- [ ] 6.1.2 执行 `cd api-test/frontend && npm run lint`
- [ ] 6.1.3 执行 `cd api-test/frontend && npm run build`

### 6.2 性能验证

- [ ] 6.2.1 对比版本对比页 DOM 数量
- [ ] 6.2.2 对比普通编辑器滚动表现
- [ ] 6.2.3 对比最大化编辑器滚动表现
- [ ] 6.2.4 对比保存/调试主线程耗时
- [ ] 6.2.5 对比 JS heap 峰值
- [ ] 6.2.6 汇总性能数据并更新排查报告

### 6.3 发布准备

- [ ] 6.3.1 每个优化方向单独提交
- [ ] 6.3.2 为虚拟树保留降级阈值
- [ ] 6.3.3 整理回滚说明
- [ ] 6.3.4 整理测试用例和验证截图

---

## 已完成基线

- [x] 核心递归函数重点路径已从 `for...in` 改为数组遍历
- [x] `recursionDelete` 已改为倒序删除并命中返回
- [x] 最大化编辑器批量操作递归参数已修正
- [x] `ComponentConfig.vue` 已完成主要步骤组件异步加载
- [x] `ScenarioDiff.vue` diff 延迟已从超长等待改为约 1 秒
- [x] `ScenarioChildDiff.vue` 已增加 interval 清理
- [x] `PluginContentAssertions.vue` 已改为 ref 查询和 RAF 清理
- [x] 重点可增删列表已改用稳定 key
- [x] 最大化编辑器步骤树已补齐 `vue-virtual-tree`
- [x] 版本对比页左右步骤树已补齐 `vue-virtual-tree`

## 当前优先级

| 优先级 | 任务 | 状态 |
|--------|------|------|
| P0 | 性能样本和基线 | 待开始 |
| P1 | `ScenarioDiff.vue` 左右树虚拟化 | 代码完成，待浏览器场景验证 |
| P2 | 强制刷新链路重构 | 代码完成，待浏览器场景验证 |
| P3 | 保存/调试深拷贝结构化 | 代码完成，待浏览器场景验证 |
| P4 | 单步骤组件内部渲染成本优化 | 待开始 |
| P5 | 大场景按需展开和按需挂载 | 待开始 |

## 本轮验证记录

- [x] `git diff --check`
- [x] `cd api-test/frontend && npm run lint`
- [x] `cd api-test/frontend && npm run build`

说明：构建仍存在项目既有 Sass deprecation warning 和 bundle size warning，本次未引入新的构建错误。

### 2026-06-22 强制刷新链路重构

- [x] 删除全局 `forceRerenderIndex` 状态、消费点和写入点
- [x] 步骤序号更新改为 Vue 2 响应式 `$set`
- [x] 主编辑器批量状态字段改为 `$set` 响应式更新，不再重建树或写入假节点
- [x] 删除最大化编辑器未使用的 `push + splice` 假节点刷新逻辑
- [x] `npm run lint`
- [x] `npm run build`

### 2026-07-07 保存/调试深拷贝结构化优化

- [x] 新增 `cloneStepTree`：递归克隆步骤树，跳过 requestResult/isBatchProcess/checkBox/showExtend/isLeaf/active 等 UI 临时字段
- [x] 新增 `buildScenarioSnapshot`：替代 EditApiScenario.vue 保存成功后和初始化时两处 scenarioDefinitionOrg 深拷贝
- [x] 新增 `cloneScenarioForDebug`：替代 ApiScenarioComponent.vue 调试时整对象深拷贝为浅拷贝
- [x] ApiAutomation.vue 版本对比深拷贝改为 cloneStepTree，元数据直接引用
- [x] api-automation.js copyScenarioRow 深拷贝改为 cloneStepTree
- [x] EditApiScenario.vue copyRow 深拷贝改为 cloneStepTree
- [x] `npm run lint`
- [x] `npm run build`
