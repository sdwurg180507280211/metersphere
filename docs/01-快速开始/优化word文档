1.将word转化为markdown文档，读取内容
pandoc "doc1.docx" -t markdown -o doc1.md

2.安装docx库创建word文档
npm install -g docx

3.检查node版本和docx包
node --version && npm list -g docx 2>&1 | head -5
v22.21.0
/home/claude/.npm-global/lib
`-- docx@9.5.1

4.创建优化后的文档
const { Document, Packer, Paragraph, TextRun, AlignmentType, HeadingLevel, LevelFormat, BorderStyle } = require('docx');
const fs = require('fs');

const doc = new Document({
  styles: {
    default: {
      document: {
        run: { font: "微软雅黑", size: 24 }
      }
    },
    paragraphStyles: [
      {
        id: "Heading1",
        name: "Heading 1",
        basedOn: "Normal",
        next: "Normal",
        quickFormat: true,
        run: { size: 36, bold: true, font: "微软雅黑", color: "1F4E78" },
        paragraph: { 
          spacing: { before: 360, after: 300 },
          outlineLevel: 0,
          alignment: AlignmentType.CENTER
        }
      }
    ]
  },
  numbering: {
    config: [
      {
        reference: "numbers",
        levels: [
          {
            level: 0,
            format: LevelFormat.DECIMAL,
            text: "%1.",
            alignment: AlignmentType.LEFT,
            style: {
              paragraph: {
                indent: { left: 720, hanging: 360 }
              }
            }
          }
        ]
      }
    ]
  },
  sections: [{
    properties: {
      page: {
        size: {
          width: 12240,
          height: 15840
        },
        margin: {
          top: 1440,
          right: 1440,
          bottom: 1440,
          left: 1440
        }
      }
    },
    children: [
      // 标题
      new Paragraph({
        heading: HeadingLevel.HEADING_1,
        children: [
          new TextRun("HCP参会承诺书")
        ]
      }),

      // 引言
      new Paragraph({
        spacing: { after: 300, line: 360 },
        children: [
          new TextRun({
            text: '本人自愿报名参加由',
            size: 24
          }),
          new TextRun({
            text: '成都康弘药业集团股份有限公司',
            size: 24,
            bold: true
          }),
          new TextRun({
            text: '(以下简称"康弘药业")支持举办的医学学术会议,并郑重承诺如下:',
            size: 24
          })
        ]
      }),

      // 第1条
      new Paragraph({
        numbering: { reference: "numbers", level: 0 },
        spacing: { before: 200, after: 200, line: 360 },
        children: [
          new TextRun({
            text: '本人身份:',
            size: 24,
            bold: true
          }),
          new TextRun({
            text: '系依法取得执业资格的医疗卫生专业人士(HCP),包括但不限于执业医师、护士、药师、医技人员、公共卫生人员或医疗机构管理人员,目前在合法注册的医疗机构、医学院校、科研单位或相关卫生机构从事专业工作。',
            size: 24
          })
        ]
      }),

      // 第2条
      new Paragraph({
        numbering: { reference: "numbers", level: 0 },
        spacing: { after: 200, line: 360 },
        children: [
          new TextRun({
            text: '信息真实:',
            size: 24,
            bold: true
          }),
          new TextRun({
            text: '所提交的姓名、执业机构、职称、联系方式等注册信息真实、准确、完整,无任何虚假或误导性内容。',
            size: 24
          })
        ]
      }),

      // 第3条
      new Paragraph({
        numbering: { reference: "numbers", level: 0 },
        spacing: { after: 200, line: 360 },
        children: [
          new TextRun({
            text: '参会目的:',
            size: 24,
            bold: true
          }),
          new TextRun({
            text: '参加本次会议仅为学习最新学术进展、交流临床经验、提升专业能力,促进宫颈疾病规范化诊疗,不以获取不当利益为目的。',
            size: 24
          })
        ]
      }),

      // 第4条
      new Paragraph({
        numbering: { reference: "numbers", level: 0 },
        spacing: { after: 200, line: 360 },
        children: [
          new TextRun({
            text: '合规行为:',
            size: 24,
            bold: true
          }),
          new TextRun({
            text: '在参会过程中,严格遵守国家法律法规、医学伦理及行业行为规范,不参与任何违反《反不正当竞争法》《医师法》或医药行业准则的行为。',
            size: 24
          })
        ]
      }),

      // 第5条
      new Paragraph({
        numbering: { reference: "numbers", level: 0 },
        spacing: { after: 200, line: 360 },
        children: [
          new TextRun({
            text: '学术用途:',
            size: 24,
            bold: true
          }),
          new TextRun({
            text: '会议期间获取的资料、课件及学术内容仅用于个人专业学习与临床参考,未经主办方书面许可,不擅自录音、录像、传播或用于商业用途。',
            size: 24
          })
        ]
      }),

      // 第6条
      new Paragraph({
        numbering: { reference: "numbers", level: 0 },
        spacing: { after: 300, line: 360 },
        children: [
          new TextRun({
            text: '责任承担:',
            size: 24,
            bold: true
          }),
          new TextRun({
            text: '如因提供虚假信息或违反上述承诺而产生的一切后果,由本人自行承担相应法律责任。',
            size: 24
          })
        ]
      }),

      // 确认段落
      new Paragraph({
        spacing: { before: 200, after: 300 },
        children: [
          new TextRun({
            text: '本人已充分知悉并同意本承诺书全部内容。',
            size: 24,
            bold: true
          })
        ]
      }),

      // 分隔线
      new Paragraph({
        spacing: { before: 200, after: 200 },
        border: {
          top: {
            color: "CCCCCC",
            space: 1,
            style: BorderStyle.SINGLE,
            size: 6
          }
        },
        children: [new TextRun("")]
      }),

      // 确认选项
      new Paragraph({
        spacing: { before: 200, after: 160 },
        children: [
          new TextRun({
            text: '☑ 我已阅读、理解并同意本《HCP参会承诺书》全部条款。',
            size: 26,
            bold: true
          })
        ]
      })
    ]
  }]
});

Packer.toBuffer(doc).then(buffer => {
  fs.writeFileSync("/mnt/user-data/outputs/康弘_HCP参会承诺书_优化版.docx", buffer);
  console.log("Document 2 created successfully!");
});

5.生成文档
cd /home/claude && node create_doc2.js