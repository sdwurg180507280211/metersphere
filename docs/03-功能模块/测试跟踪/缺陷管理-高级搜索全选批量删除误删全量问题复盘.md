# 缺陷管理：高级搜索后“全选批量删除”误删全量问题复盘

## 1. 现象

页面：`测试跟踪 → 缺陷管理 → 缺陷列表（IssueList）`

- **操作**：高级搜索/过滤后 → 点击全选 → 批量删除
- **实际**：删除项目/工作空间下全部缺陷（误删）
- **预期**：只删除“当前筛选结果”的全部缺陷


## 2. 原理（为什么必须传 name/filters/combine/unSelectIds/moduleIds/nodeIds）

MsTable 的“全选”不是把所有行 ID 发给后端（跨页做不到），而是发一套**查询条件**，让后端算出全集。

- **非全选（selectAll=false）**：`S = batchDeleteIds`
- **全选（selectAll=true）**：`S = Q(name, filters, combine, moduleIds, nodeIds) - unSelectIds`

其中：
- `Q(...)`：按当前列表筛选条件查询得到的全部缺陷集合
- `unSelectIds`：全选后取消勾选的 ID（差集剔除）


## 3. 代码对比（原来 vs 改完）

### 3.1 前端：`IssueList.vue`

文件：`test-track/frontend/src/business/issue/IssueList.vue`

修复前：
```js
batchDeleteIssue({"batchDeleteIds": selectIds, "batchDeleteAll": this.page.condition.selectAll})
```

修复后：
```js
batchDeleteIssue({
  "batchDeleteIds": selectIds,
  "batchDeleteAll": this.page.condition.selectAll,
  "unSelectIds": this.page.condition.unSelectIds,
  "moduleIds": this.page.condition.moduleIds,
  "nodeIds": this.page.condition.nodeIds,
  "filters": this.page.condition.filters,
  "combine": this.page.condition.combine,
  "name": this.page.condition.name,
})
```


### 3.2 后端：`IssuesService.batchDelete`

文件：`test-track/backend/src/main/java/io/metersphere/service/IssuesService.java`

修复前（核心问题：忽略筛选条件，导致全量删除）：
```java
if (request.getBatchDeleteAll()) {
    IssuesRequest issuesRequest = new IssuesRequest();
    issuesRequest.setWorkspaceId(SessionUtils.getCurrentWorkspaceId());
    issuesRequest.setProjectId(SessionUtils.getCurrentProjectId());
    List<IssuesDao> issuesDaos = listByWorkspaceId(issuesRequest);
    if (CollectionUtils.isNotEmpty(issuesDaos)) {
        issuesDaos.forEach(issuesDao -> delete(issuesDao.getId()));
    }
}
```

修复后（按 Q(...) 计算范围，再删除）：
```java
if (request.getBatchDeleteAll()) {
    IssuesRequest issuesRequest = new IssuesRequest();
    issuesRequest.setWorkspaceId(SessionUtils.getCurrentWorkspaceId());
    issuesRequest.setProjectId(SessionUtils.getCurrentProjectId());
    issuesRequest.setSelectAll(true);
    issuesRequest.setUnSelectIds(request.getUnSelectIds());
    issuesRequest.setModuleIds(request.getModuleIds());
    issuesRequest.setNodeIds(request.getNodeIds());
    issuesRequest.setName(request.getName());
    issuesRequest.setFilters(request.getFilters());
    issuesRequest.setCombine(request.getCombine());

    List<IssuesDao> issuesDaos = listByWorkspaceId(issuesRequest);
    if (CollectionUtils.isNotEmpty(issuesDaos)) {
        issuesDaos.forEach(issuesDao -> delete(issuesDao.getId()));
    }
}
```


### 3.3 DTO：`IssuesUpdateRequest`

文件：`framework/sdk-parent/xpack-interface/src/main/java/io/metersphere/xpack/track/dto/request/IssuesUpdateRequest.java`

修复前：
```java
private List<String> batchDeleteIds;
private Boolean batchDeleteAll;
```

修复后：
```java
private List<String> batchDeleteIds;
private Boolean batchDeleteAll;

private String name;
private Map<String, List<String>> filters;
private Map<String, Object> combine;
private List<String> unSelectIds;
private List<String> moduleIds;
private List<String> nodeIds;
```


## 4. 影响范围

- **影响页面**：缺陷管理列表页（IssueList）的批量删除
- **不影响**：单条删除（`/issues/delete/{id}`）


## 5. 验证用例

1. **有筛选条件**：高级搜索/过滤 → 全选 → 批量删除 → 只删除筛选结果
2. **全选后取消勾选**：筛选 → 全选 → 取消勾选 1~2 条 → 批量删除 → 被取消的不会删除
