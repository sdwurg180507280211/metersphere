# 分析统计微服务实施记录

## 实施时间
2026-01-30

## 实施概述
成功创建了独立的分析统计微服务模块（analytics-stat），采用微前端架构，前端使用Vue 3 + Vite + TypeScript，后端使用Spring Boot 3.2.12 + Java 17。

## 模块信息

### 基本信息
- **模块名称**: analytics-stat（分析统计）
- **服务端口**: 8009 (HTTP), 7009 (管理端口)
- **前端开发端口**: 4009
- **Eureka服务名**: analytics-stat

### 功能范围
1. 综合查询
2. SQL查询台
3. 统计图表
4. 数据字典

## 技术栈

### 后端技术栈
- Spring Boot 3.2.12
- Java 17
- MyBatis 3.0.3
- Spring Cloud 2023.0.1
- Eureka Client（服务注册）
- Flyway（数据库迁移）

### 前端技术栈（激进升级）
- Vue 3.4.0
- Vite 5.0.0
- TypeScript 5.3.0
- Element Plus 2.5.0
- Vue Router 4.2.5
- Pinia 2.1.7
- ECharts 5.4.3
- Monaco Editor 0.45.0

## 项目结构

```
analytics-stat/
├── backend/
│   ├── src/main/
│   │   ├── java/io/metersphere/analyticsstat/
│   │   │   ├── AnalyticsStatApplication.java    # 启动类
│   │   │   └── controller/
│   │   │       └── HealthController.java        # 健康检查
│   │   └── resources/
│   │       ├── application.properties           # 配置文件
│   │       └── db/migration/
│   │           └── V1__init_analytics_stat.sql  # 初始化脚本
│   ├── pom.xml
│   └── .gitignore
├── frontend/
│   ├── src/
│   │   ├── views/
│   │   │   ├── Layout.vue                       # 布局组件
│   │   │   ├── Dashboard.vue                    # 数据概览
│   │   │   ├── SqlConsole.vue                   # SQL查询台
│   │   │   └── DataDictionary.vue               # 数据字典
│   │   ├── router/
│   │   │   └── index.ts                         # 路由配置
│   │   ├── App.vue                              # 根组件
│   │   ├── main.ts                              # 入口文件
│   │   └── env.d.ts                             # 类型声明
│   ├── index.html
│   ├── vite.config.ts                           # Vite配置
│   ├── tsconfig.json                            # TypeScript配置
│   ├── package.json
│   ├── pom.xml                                  # Maven前端插件
│   └── .gitignore
├── pom.xml                                      # 父POM
└── Dockerfile                                   # Docker镜像构建
```

## 数据库设计

### 表结构

#### 1. sql_query_history（SQL查询历史记录表）
```sql
CREATE TABLE `sql_query_history` (
    `id` VARCHAR(50) NOT NULL COMMENT '主键ID',
    `user_id` VARCHAR(50) NOT NULL COMMENT '用户ID',
    `query_sql` TEXT NOT NULL COMMENT '查询SQL',
    `query_time` BIGINT NOT NULL COMMENT '查询时间(ms)',
    `result_count` INT DEFAULT 0 COMMENT '结果行数',
    `status` VARCHAR(20) NOT NULL COMMENT '执行状态: SUCCESS/FAILED',
    `error_message` TEXT COMMENT '错误信息',
    `create_time` BIGINT NOT NULL COMMENT '创建时间',
    PRIMARY KEY (`id`),
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 2. data_dictionary（数据字典表）
```sql
CREATE TABLE `data_dictionary` (
    `id` VARCHAR(50) NOT NULL COMMENT '主键ID',
    `dict_type` VARCHAR(100) NOT NULL COMMENT '字典类型',
    `dict_code` VARCHAR(100) NOT NULL COMMENT '字典编码',
    `dict_value` VARCHAR(500) NOT NULL COMMENT '字典值',
    `dict_label` VARCHAR(200) NOT NULL COMMENT '字典标签',
    `sort_order` INT DEFAULT 0 COMMENT '排序',
    `enabled` TINYINT(1) DEFAULT 1 COMMENT '是否启用',
    `remark` VARCHAR(500) COMMENT '备注',
    `create_time` BIGINT NOT NULL COMMENT '创建时间',
    `update_time` BIGINT NOT NULL COMMENT '更新时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_type_code` (`dict_type`, `dict_code`),
    INDEX `idx_dict_type` (`dict_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

## 配置修改

### 1. 根pom.xml
在 `<modules>` 中添加：
```xml
<module>analytics-stat</module>
```

### 2. Gateway配置
在 `framework/gateway/src/main/resources/application.properties` 中添加：
```properties
springdoc.swagger-ui.urls[10].url=/analytics-stat/v3/api-docs
springdoc.swagger-ui.urls[10].name=analytics-stat
```

## 编译验证

### 后端编译
```bash
./mvnw clean compile -pl analytics-stat/backend -am -DskipAntRunForJenkins=true
```

**结果**: ✅ BUILD SUCCESS

### 后端打包
```bash
./mvnw clean package -pl analytics-stat/backend -am -DskipTests -DskipAntRunForJenkins=true
```

**结果**: ✅ BUILD SUCCESS

生成的JAR文件：
- 位置：`analytics-stat/backend/target/analytics-stat-2.10.jar`
- 类型：可执行Spring Boot JAR
- 大小：约50MB（包含所有依赖）

### 启动服务
```bash
cd analytics-stat/backend/target
java -jar analytics-stat-2.10.jar
```

或使用Maven插件：
```bash
cd analytics-stat/backend
mvn spring-boot:run
```

### 前端开发
```bash
cd analytics-stat/frontend
npm install
npm run dev
```

访问地址: http://localhost:4009

## 页面功能

### 1. 数据概览（Dashboard）
- 欢迎页面
- 功能模块快速入口
- 三个功能卡片：SQL查询台、数据字典、统计图表

### 2. SQL查询台（SqlConsole）
- SQL编辑器（Textarea）
- 执行查询按钮
- 查询结果表格展示
- 空状态提示

### 3. 数据字典（DataDictionary）
- 字典列表展示
- 新增/编辑/删除操作
- 状态标签显示
- 空状态提示

## 微前端集成

### qiankun配置（待实施）
需要在主应用中注册analytics-stat子应用：

```javascript
// framework/sdk-parent/frontend/src/micro-app.js
{
  name: 'analytics-stat',
  entry: '//localhost:4009',
  container: '#subapp-viewport',
  activeRule: '/analytics-stat',
}
```

## Docker镜像

### Dockerfile特点
- 基于 `fabric8-java-alpine-openjdk17-jre`
- 使用分层JAR方式优化镜像大小
- 支持多架构构建（amd64、arm64）

### 构建命令
```bash
cd analytics-stat
docker build -t analytics-stat:2.10 .
```

## 下一步工作

### 1. 前端开发
- [ ] 安装npm依赖
- [ ] 实现SQL查询台后端API调用
- [ ] 实现数据字典CRUD功能
- [ ] 集成Monaco Editor作为SQL编辑器
- [ ] 添加查询历史记录功能

### 2. 后端开发
- [ ] 实现SQL查询台Service和Controller
- [ ] 实现数据字典Service和Controller
- [ ] 添加权限控制
- [ ] 添加SQL注入防护
- [ ] 实现查询结果分页

### 3. 微前端集成
- [ ] 在主应用中注册analytics-stat子应用
- [ ] 配置路由和菜单
- [ ] 测试子应用加载和卸载
- [ ] 实现主子应用通信

### 4. 部署配置
- [ ] 配置Eureka服务注册
- [ ] 配置Gateway路由规则
- [ ] 添加到Docker Compose
- [ ] 配置Nginx反向代理

### 5. 测试
- [ ] 单元测试
- [ ] 集成测试
- [ ] 前端E2E测试
- [ ] 性能测试

## 与工作流微服务的配置对比

### 相同点
1. **Spring Boot配置**：
   - 都使用Spring Boot 3.2.12
   - 都排除了QuartzAutoConfiguration、LdapAutoConfiguration、Neo4jAutoConfiguration
   - 都配置了@PropertySource加载外部配置
   - 都使用@ServletComponentScan
   - 都配置了@EnableConfigurationProperties

2. **Maven配置**：
   - 都依赖io.metersphere:sdk
   - 都配置了spring-boot-maven-plugin
   - 都配置了mainClass
   - 都配置了loaderImplementation=CLASSIC

3. **Flyway配置**：
   - 都启用了Flyway数据库迁移
   - 都配置了baseline-on-migrate=true

### 差异点
1. **端口配置**：
   - workflow: 8008 (HTTP), 7008 (管理)
   - analytics-stat: 8009 (HTTP), 7009 (管理)

2. **特殊依赖**：
   - workflow: 依赖flowable-spring-boot-starter-process（工作流引擎）
   - analytics-stat: 无特殊依赖（通用微服务）

3. **数据源配置**：
   - workflow: 使用自定义数据源配置 `${workflow.datasource.url}`
   - analytics-stat: 使用默认数据源配置

4. **前端集成**：
   - workflow: 仅后端服务（无前端模块）
   - analytics-stat: 包含前端模块（Vue 3 + Vite）

### 配置完整性检查
参考workflow-service的配置，analytics-stat已补充：
- ✅ 排除RsaConfig、PermissionConfig、OpenApiConfig
- ✅ 配置@PropertySource加载commons.properties
- ✅ 配置@EnableConfigurationProperties（KafkaProperties、MinioProperties）
- ✅ 配置spring-boot-maven-plugin的mainClass
- ✅ 配置@ServletComponentScan
- ✅ 配置@EnableDiscoveryClient（Eureka服务注册）

### 1. 前端技术升级
- 采用Vue 3 Composition API，代码更简洁
- 使用Vite构建，开发体验更好（HMR秒级）
- TypeScript提供类型安全
- Element Plus组件库更现代

### 2. 微前端隔离
- 前端技术栈独立，不影响其他模块
- 可以独立开发、测试、部署
- 技术债务隔离，便于后续升级

### 3. 后端架构一致
- 保持与现有系统一致的技术栈
- 复用SDK和共享类库
- 统一的配置和日志管理

## 资源评估

### 内存占用
- 预估：2-4GB（与report-stat相当）
- JVM堆内存：-Xms1g -Xmx2g
- 元空间：-XX:MetaspaceSize=256m -XX:MaxMetaspaceSize=512m

### CPU占用
- 正常运行：1-2核
- 高负载（大量查询）：2-4核

### 磁盘占用
- 应用程序：~200MB
- 日志文件：根据配置，建议限制在1GB以内
- 数据库：根据使用情况增长

## 注意事项

### 1. SQL查询台安全
- 必须实现严格的权限控制
- 禁止执行DDL和DML语句（仅允许SELECT）
- 添加SQL注入防护
- 限制查询结果集大小
- 记录所有查询操作日志

### 2. 性能优化
- 查询超时控制（建议30秒）
- 结果集分页（建议每页1000条）
- 查询历史记录定期清理
- 考虑添加查询缓存

### 3. 前端开发
- 使用Monaco Editor替代Textarea
- 添加SQL语法高亮和自动补全
- 实现查询结果导出功能（CSV、Excel）
- 添加查询模板功能

## 参考文档
- [模块命名规范](./模块命名规范.md)
- [前端技术栈选型](./前端技术栈选型.md)
- [后端技术栈选型](./后端技术栈选型.md)
- [资源占用评估](./资源占用评估.md)

## 总结

成功创建了分析统计微服务的完整骨架，包括：
- ✅ 后端Spring Boot项目结构
- ✅ 前端Vue 3 + Vite项目结构
- ✅ 数据库初始化脚本
- ✅ Maven构建配置
- ✅ Docker镜像配置
- ✅ 基础页面组件
- ✅ 路由配置
- ✅ 健康检查接口
- ✅ 后端编译验证通过

项目已具备开发条件，可以开始实现具体业务功能。


## 7. 前端资源集成（2026-02-02）

### 7.1 问题发现
- 前端资源已构建到`dist`目录，但未复制到backend的`static`和`public`目录
- 访问`http://127.0.0.1:8009/analytics-stat/`返回404

### 7.2 解决方案
在`analytics-stat/backend/pom.xml`中已有`maven-antrun-plugin`配置：
```xml
<plugin>
    <groupId>org.apache.maven.plugins</groupId>
    <artifactId>maven-antrun-plugin</artifactId>
    <executions>
        <execution>
            <id>main-class-placement</id>
            <phase>generate-resources</phase>
            <configuration>
                <skip>${skipAntRunForJenkins}</skip>
                <target>
                    <copy todir="src/main/resources/static">
                        <fileset dir="../frontend/dist">
                            <exclude name="*.html"/>
                        </fileset>
                    </copy>
                    <copy todir="src/main/resources/public">
                        <fileset dir="../frontend/dist">
                            <include name="*.html"/>
                        </fileset>
                    </copy>
                </target>
            </configuration>
            <goals>
                <goal>run</goal>
            </goals>
        </execution>
    </executions>
</plugin>
```

### 7.3 重新构建
```bash
./mvnw clean package -DskipTests -pl analytics-stat/backend
```

构建日志显示：
- `[copy] Copying 13 files to .../static` - 复制了13个静态资源文件
- `[copy] Copying 1 file to .../public` - 复制了1个index.html文件

### 7.4 验证结果
重启服务后验证：
```bash
# 健康检查
curl http://127.0.0.1:8009/analytics-stat/health
# 返回：{"service":"analytics-stat","description":"分析统计微服务","version":"1.0.0","status":"UP"}

# 前端页面
curl http://127.0.0.1:8009/
# 返回：完整的HTML页面，包含Vue应用和qiankun配置

# 静态资源
curl -I http://127.0.0.1:8009/assets/index-BOFi7fDo.js
# 返回：HTTP/1.1 200 OK
```

### 7.5 Spring Boot静态资源机制
Spring Boot会自动提供以下位置的静态资源：
- `classpath:/static/` - 静态资源（JS、CSS、图片等）
- `classpath:/public/` - 公共资源（index.html等）
- `classpath:/resources/` - 其他资源
- `classpath:/META-INF/resources/` - META-INF资源

访问规则：
- `/` 或 `/index.html` → `public/index.html`
- `/assets/*` → `static/assets/*`

### 7.6 构建流程总结
1. **前端构建**：`npm run build` → 生成`dist`目录
2. **Maven复制**：`maven-antrun-plugin` → 复制到`backend/src/main/resources/`
   - `dist/*.html` → `public/` （Spring Boot会作为欢迎页）
   - `dist/assets/*` → `static/assets/` （静态资源）
3. **Maven打包**：`spring-boot-maven-plugin` → 打包到JAR的`BOOT-INF/classes/`

### 7.7 访问路径
- 主页：`http://127.0.0.1:8009/` → `public/index.html`
- 静态资源：`http://127.0.0.1:8009/assets/*` → `static/assets/*`
- API接口：`http://127.0.0.1:8009/analytics-stat/*`

## 8. 当前状态（2026-02-02）

### 已完成
- ✅ 后端Spring Boot项目结构
- ✅ 前端Vue 3 + Vite项目结构
- ✅ 数据库初始化脚本
- ✅ Maven构建配置
- ✅ Docker镜像配置
- ✅ 基础页面组件
- ✅ 路由配置
- ✅ 健康检查接口
- ✅ 后端编译验证通过
- ✅ 前端构建成功
- ✅ 前端资源集成到后端JAR
- ✅ 服务启动成功（端口8009）
- ✅ 前端页面可访问（http://127.0.0.1:8009/）
- ✅ qiankun微前端配置完成

### 下一步计划
- [ ] 验证Eureka注册
- [ ] 验证Gateway路由
- [ ] 验证qiankun集成（通过主应用访问）
- [ ] 添加菜单入口（可选）
- [ ] 实现认证拦截器
- [ ] 实现SQL查询台和数据字典的后端API
