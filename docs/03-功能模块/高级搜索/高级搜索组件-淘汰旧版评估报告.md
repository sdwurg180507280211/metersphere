# 高级搜索组件 - 淘汰旧版评估报告

## 使用情况统计

### 组件使用次数

| 组件 | 使用次数 | 占比 |
|------|---------|------|
| **旧版** MsTableAdvSearchBar | 63次 | 88.7% |
| **新版** MsTableAdvSearch | 8次 | 11.3% |
| **合计** | 71次 | 100% |

### 结论

**不建议立即淘汰旧版组件**，原因如下：

1. **使用范围广**：旧版组件在63个页面中使用，占比88.7%
2. **迁移成本高**：需要修改63个Vue文件
3. **风险较大**：涉及核心业务模块（测试跟踪、接口测试、性能测试等）
4. **功能稳定**：旧版组件运行稳定，无明显缺陷

## 使用分布

### 按模块统计

| 模块 | 旧版使用次数 | 新版使用次数 |
|------|------------|------------|
| test-track（测试跟踪） | ~30 | 1 |
| api-test（接口测试） | ~15 | 0 |
| performance-test（性能测试） | ~5 | 0 |
| project-management（项目管理） | ~5 | 0 |
| workstation（工作台） | ~3 | 0 |
| framework/sdk-parent | ~5 | 7 |

### 典型使用场景

#### 旧版组件（MsTableAdvSearchBar）

```vue
<!-- 测试用例关联列表 -->
<ms-table-adv-search-bar 
  :condition.sync="condition" 
  @search="search" 
/>

<!-- 接口定义列表 -->
<ms-table-adv-search-bar 
  :condition.sync="condition" 
  @search="initTable" 
/>

<!-- 性能测试列表 -->
<ms-table-adv-search-bar 
  :condition.sync="condition" 
  @search="search" 
/>
```

#### 新版组件（MsTableAdvSearch）

```vue
<!-- 测试用例列表（新UI） -->
<ms-table-adv-search 
  :condition.sync="condition" 
  @search="search" 
  ref="advanceSearch"
/>
```

## 淘汰方案

### 方案1：保持现状（推荐）

**策略**：
- 保留旧版组件，继续维护
- 新开发页面使用新版组件
- 逐步自然迁移，不强制替换

**优点**：
- ✅ 零风险
- ✅ 无需投入人力
- ✅ 不影响现有功能

**缺点**：
- ❌ 需要维护两套代码
- ❌ 迁移周期长

**适用场景**：
- 当前阶段（功能稳定期）
- 人力资源有限
- 风险控制优先

---

### 方案2：渐进式迁移

**策略**：
- 制定迁移计划，分模块逐步替换
- 优先迁移新开发或重构的页面
- 设定迁移时间表（如6-12个月）

**实施步骤**：

#### 第一阶段：准备（1个月）
1. 确保新版组件功能完全覆盖旧版
2. 编写迁移指南和自动化脚本
3. 在测试环境验证新版组件稳定性

#### 第二阶段：试点迁移（2个月）
1. 选择3-5个低风险页面试点
2. 收集反馈，优化新版组件
3. 总结迁移经验

#### 第三阶段：批量迁移（6个月）
1. 按模块分批迁移
2. 每批迁移后进行回归测试
3. 及时修复问题

#### 第四阶段：清理（1个月）
1. 确认所有页面已迁移
2. 删除旧版组件代码
3. 更新文档

**优点**：
- ✅ 风险可控
- ✅ 可以及时发现问题
- ✅ 最终统一技术栈

**缺点**：
- ❌ 需要投入较多人力
- ❌ 周期较长

**适用场景**：
- 有明确的技术债务清理计划
- 有专门的重构时间窗口
- 团队人力充足

---

### 方案3：一次性替换（不推荐）

**策略**：
- 一次性替换所有63个文件
- 集中测试验证

**优点**：
- ✅ 快速统一技术栈
- ✅ 一次性解决问题

**缺点**：
- ❌ 风险极高
- ❌ 测试工作量巨大
- ❌ 容易引入新问题

**适用场景**：
- 不推荐使用

---

## 迁移成本估算

### 人力成本

| 任务 | 工作量 | 说明 |
|------|--------|------|
| 功能对齐 | 2人天 | 确保新版组件功能完全覆盖旧版 |
| 编写迁移脚本 | 3人天 | 自动化替换import和组件名 |
| 手动调整 | 15人天 | 处理特殊情况（63个文件 × 0.25天） |
| 测试验证 | 20人天 | 回归测试所有相关页面 |
| 问题修复 | 5人天 | 修复迁移过程中发现的问题 |
| **合计** | **45人天** | 约2个月（2人并行） |

### 风险成本

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| 功能不兼容 | 中 | 高 | 充分测试，保留回滚方案 |
| 性能问题 | 低 | 中 | 性能测试，优化代码 |
| 用户体验变化 | 中 | 低 | 提前通知用户，收集反馈 |
| 引入新Bug | 中 | 高 | 完整回归测试 |

---

## 技术对比

### 功能对比

| 功能 | 旧版 | 新版 | 说明 |
|------|------|------|------|
| 基础搜索 | ✅ | ✅ | 功能一致 |
| 自定义字段 | ✅ | ✅ | 功能一致 |
| 动态添加条件 | ✅ | ✅ | 功能一致 |
| UI样式 | 文字链接 | 图标按钮 | 新版更现代 |
| 响应式布局 | ✅ | ✅ | 功能一致 |
| Tab键快捷键 | ✅ | ✅ | 已同步实现 |

### 代码质量对比

| 指标 | 旧版 | 新版 |
|------|------|------|
| 代码行数 | ~400行 | ~450行 |
| 组件复杂度 | 中 | 中 |
| 可维护性 | 良好 | 良好 |
| 性能 | 良好 | 良好 |

**结论**：两个组件在功能和代码质量上基本一致，主要区别在UI样式。

---

## 建议

### 短期（当前）

**保持现状，不进行淘汰**

理由：
1. 旧版组件功能稳定，无明显缺陷
2. 使用范围广，迁移成本高
3. 当前已实现Tab键快捷键功能，两个组件功能对齐
4. 通过全局单例模式解决了多组件冲突问题

### 中期（6-12个月）

**渐进式迁移**

建议：
1. 新开发页面强制使用新版组件
2. 重构页面时顺便迁移到新版组件
3. 不强制要求旧页面迁移

### 长期（1-2年）

**完全淘汰旧版组件**

前提条件：
1. 新版组件经过充分验证
2. 大部分页面已自然迁移
3. 剩余页面数量少于10个
4. 有专门的技术债务清理计划

---

## 迁移指南（供参考）

### 自动化替换脚本

```bash
#!/bin/bash
# 批量替换组件引用

# 1. 替换import语句
find . -name "*.vue" -type f -exec sed -i '' \
  's/MsTableAdvSearchBar/MsTableAdvSearch/g' {} \;

# 2. 替换组件路径
find . -name "*.vue" -type f -exec sed -i '' \
  's/components\/search\/MsTableAdvSearchBar/components\/new-ui\/MsTableAdvSearch/g' {} \;

# 3. 替换组件标签
find . -name "*.vue" -type f -exec sed -i '' \
  's/<ms-table-adv-search-bar/<ms-table-adv-search/g' {} \;

find . -name "*.vue" -type f -exec sed -i '' \
  's/<\/ms-table-adv-search-bar>/<\/ms-table-adv-search>/g' {} \;
```

### 手动调整清单

迁移后需要手动检查的项目：

1. **组件属性**
   - 检查是否有旧版特有的属性
   - 确认新版组件支持所有使用的属性

2. **事件监听**
   - 确认事件名称一致
   - 检查事件参数格式

3. **样式调整**
   - 新版组件UI不同，可能需要调整布局
   - 检查按钮位置和间距

4. **功能测试**
   - 测试基础搜索功能
   - 测试高级搜索功能
   - 测试自定义字段搜索
   - 测试Tab键快捷键

---

## 附录：受影响文件清单

### 核心模块

#### test-track（测试跟踪）
- `business/case/components/TestCaseRelateApiList.vue`
- `business/case/components/TestCaseRelateLoadList.vue`
- `business/case/components/TestCaseRelateScenarioList.vue`
- `business/case/components/TestCaseRelateUiScenarioList.vue`
- `business/case/components/case/relate/*.vue`（多个文件）
- `business/plan/view/comonents/**/*.vue`（多个文件）
- `business/review/view/components/TestReviewRelevance.vue`
- 等约30个文件

#### api-test（接口测试）
- `business/definition/components/list/ApiList.vue`
- `business/definition/components/list/ApiCaseSimpleList.vue`
- `business/automation/scenario/ApiScenarioList.vue`
- `business/automation/scenario/api/RelevanceScenarioList.vue`
- `business/automation/scenario/api/RelevanceCaseList.vue`
- 等约15个文件

#### performance-test（性能测试）
- 约5个文件

#### project-management（项目管理）
- `business/menu/environment/components/ui-related/UiScenarioList.vue`
- `business/menu/environment/components/ui-related/UiCustomCommandList.vue`
- `business/menu/file/header/FileHeader.vue`
- 等约5个文件

#### framework/sdk-parent（共享组件）
- `components/MsTableHeader.vue`
- `components/search/MsSearch.vue`
- `components/environment/**/*.vue`（多个文件）
- 等约5个文件

---

## 总结

### 当前决策

**不淘汰旧版组件，保持现状**

### 理由

1. **使用范围广**：63个页面使用，占比88.7%
2. **功能稳定**：运行稳定，无明显缺陷
3. **成本高**：迁移需要45人天，约2个月
4. **风险大**：涉及核心业务模块
5. **已解决冲突**：通过全局单例模式解决了多组件冲突问题

### 未来规划

- **短期**：保持现状，新页面使用新版
- **中期**：渐进式迁移，重构时顺便替换
- **长期**：完全淘汰旧版（1-2年后）

---

## 修改日期

2025-01-22

## 修改人

Kiro AI Assistant
