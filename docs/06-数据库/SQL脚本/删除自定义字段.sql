SET SESSION innodb_lock_wait_timeout = 7200;

-- ============================================
-- 删除缺陷模板中"严重级别"字段的关联
-- ============================================
-- 目的：从所有缺陷模板中移除"严重级别"字段
-- 策略选择：根据需求选择不同的删除级别
--   级别1：仅删除模板关联（推荐）- 字段不再显示，但保留历史数据
--   级别2：删除模板关联 + 历史数据 - 彻底清除该字段的所有痕迹
--   级别3：完全删除 - 包括字段定义本身

-- ============================================
-- 级别1：删除所有模板中"严重级别"字段的关联（推荐）
-- ============================================
-- 影响：
--   ✓ 新建/编辑缺陷时不再显示"严重级别"字段
--   ✓ 保留已存在缺陷的严重级别数据（可查询）
--   ✓ 保留字段定义（可随时恢复关联）
-- 适用场景：暂时不需要该字段，但可能将来恢复使用

DELETE FROM custom_field_template
WHERE field_id = 'issue-severity-level-field-2024-12-01-002';

-- ============================================
-- 级别2：删除已存在缺陷的"严重级别"数据（可选）
-- ============================================
-- 影响：
--   ✓ 永久删除所有缺陷的严重级别历史数据
--   ✗ 数据不可恢复
-- 适用场景：确定不再需要该字段的历史数据
-- 使用方法：取消下面的注释

-- DELETE FROM custom_field_issues
-- WHERE field_id = 'issue-severity-level-field-2024-12-01-002';

-- ============================================
-- 级别3：删除"严重级别"字段定义（可选）
-- ============================================
-- 影响：
--   ✓ 彻底删除字段定义
--   ✗ 无法通过界面恢复，需要重新执行 V143 脚本
-- 适用场景：确定永久废弃该字段
-- 使用方法：取消下面的注释

-- DELETE FROM custom_field
-- WHERE id = 'issue-severity-level-field-2024-12-01-002';

-- ============================================
-- 验证脚本（可选）
-- ============================================
-- 执行后可运行以下 SQL 验证删除结果：
-- 
-- -- 1. 检查模板关联是否已删除（应返回 0 行）
-- SELECT COUNT(*) as template_count 
-- FROM custom_field_template 
-- WHERE field_id = 'issue-severity-level-field-2024-12-01-002';
-- 
-- -- 2. 检查历史数据是否保留（如果执行了级别2，应返回 0 行）
-- SELECT COUNT(*) as data_count 
-- FROM custom_field_issues 
-- WHERE field_id = 'issue-severity-level-field-2024-12-01-002';
-- 
-- -- 3. 检查字段定义是否保留（如果执行了级别3，应返回 0 行）
-- SELECT COUNT(*) as field_count 
-- FROM custom_field 
-- WHERE id = 'issue-severity-level-field-2024-12-01-002';

-- 恢复默认锁等待时间
SET SESSION innodb_lock_wait_timeout = DEFAULT;
