flowchart TB
    %% ========== 入口 ==========
    START([修改了代码，如何构建？]) --> Q1{修改了什么？}

    %% ========== 前端分支 ==========
    Q1 -->|前端代码| Q_FE{改的是哪个前端？}

    Q_FE -->|业务模块前端<br/>如 test-track/frontend| FE1
    subgraph FE1[业务模块前端构建]
        direction TB
        FE1_1["cd 该模块/frontend"] --> FE1_2["npm run build"]
    end
    FE1 --> FE_RESTART[重启: 仅该模块后端]

    Q_FE -->|共享前端SDK<br/>framework/sdk-parent/frontend| FE2
    subgraph FE2[共享前端SDK构建]
        direction TB
        FE2_1["cd framework/sdk-parent/frontend"] --> FE2_2["npm run build"]
        FE2_2 --> FE2_3["各业务模块前端重新 npm install"]
    end
    FE2 --> FE_RESTART_ALL[重启: 所有后端服务]

    %% ========== 后端分支 ==========
    Q1 -->|后端代码| Q_BE{改的是哪个模块？}

    %% --- 业务模块自身代码 ---
    Q_BE -->|业务模块自身代码<br/>Service/Controller/Mapper| BE1
    subgraph BE1[业务模块构建]
        direction TB
        BE1_1["./mvnw clean package -DskipTests<br/>-pl 该模块/backend -am"]
    end
    BE1 --> BE1_RESTART[重启: 仅该业务服务]

    %% --- domain ---
    Q_BE -->|framework/sdk-parent/domain| BE2
    subgraph BE2[domain 变更构建]
        direction TB
        BE2_1["./mvnw clean install -N"] --> BE2_2["./mvnw clean install -DskipTests<br/>-pl framework,framework/sdk-parent,<br/>framework/sdk-parent/domain,<br/>framework/sdk-parent/sdk,<br/>framework/sdk-parent/xpack-interface,<br/>framework/sdk-parent/jmeter"]
        BE2_2 --> BE2_3["./mvnw clean package -DskipTests<br/>-pl 修改的业务模块/backend -am"]
        BE2_3 --> BE2_4["./mvnw clean package -DskipTests<br/>-pl framework/gateway -am"]
    end
    BE2 --> BE2_RESTART[重启: gateway + 修改的业务模块]

    %% --- sdk ---
    Q_BE -->|framework/sdk-parent/sdk| BE3
    subgraph BE3[sdk 变更构建]
        direction TB
        BE3_1["同 domain 变更的4步"]
    end
    BE3 --> BE3_RESTART[重启: gateway + 修改的业务模块]

    %% --- xpack-interface ---
    Q_BE -->|framework/sdk-parent/xpack-interface| BE4
    subgraph BE4[xpack-interface 变更构建]
        direction TB
        BE4_1["./mvnw clean install -DskipTests<br/>-pl framework/sdk-parent/xpack-interface"]
        BE4_1 --> BE4_2["./mvnw clean package -DskipTests<br/>-pl 修改的业务模块/backend -am"]
    end
    BE4 --> BE4_RESTART[重启: 仅修改的业务模块]

    %% --- gateway ---
    Q_BE -->|framework/gateway| BE5
    subgraph BE5[gateway 构建]
        direction TB
        BE5_1["./mvnw clean package -DskipTests<br/>-pl framework/gateway -am"]
    end
    BE5 --> BE5_RESTART[重启: 仅 gateway]

    %% --- eureka ---
    Q_BE -->|framework/eureka| BE6
    subgraph BE6[eureka 构建]
        direction TB
        BE6_1["./mvnw clean package -DskipTests<br/>-pl framework/eureka -am"]
    end
    BE6 --> BE6_RESTART[重启: eureka]

    %% --- 数据库 Migration ---
    Q1 -->|数据库 Migration SQL| DB1
    subgraph DB1[Migration 构建]
        direction TB
        DB1_1{新增还是修改？}
        DB1_1 -->|新增 SQL 文件| DB1_NEW["无需编译<br/>重启服务 Flyway 自动执行"]
        DB1_1 -->|修改已有 SQL| DB1_MOD["需手动处理<br/>Flyway checksum"]
    end

    %% ========== 基础概念：Maven 生命周期阶段 ==========
    subgraph CONCEPT[Maven 生命周期：后一阶段自动包含前面所有阶段]
        direction LR
        M1["① compile 编译<br/>────────────────────<br/>src/main/java/*.java<br/>        ↓ javac<br/>target/classes/*.class"] --> M2["② package 打包<br/>────────────────────<br/>先执行 compile<br/>        ↓ jar<br/>target/xxx.jar"]
        M2 --> M3["③ install 安装<br/>────────────────────<br/>先执行 compile + package<br/>        ↓ copy<br/>~/.m2/repository/<br/>io/metersphere/domain/<br/>2.10/domain-2.10.jar"]
    end

    subgraph CONCEPT_Q[A: install 包含 package 吗？]
        direction TB
        CA1["是的，install = compile + package + 复制到 .m2"] --> CA2["只需执行 install，<br/>不需要先 package 再 install"]
    end

    subgraph CONCEPT_RULE[B: 什么时候用哪个？]
        direction TB
        CB1["install → 被别人依赖的模块<br/>如 domain / sdk / xpack-interface"] --> CB3["这样其他模块编译时<br/>才能从 .m2 拿到新版本"]
        CB2["package → 最终业务模块<br/>如 test-track / gateway"] --> CB4["没人依赖它们<br/>不需要装到 .m2"]
    end
    subgraph CHECK[速查：是否需要重新编译 gateway？]
        direction TB
        C1["改了 domain 或 sdk"] --> C_YES["是，必须重新编译 gateway"]
        C2["改了业务模块自身代码"] --> C_NO["否，只编译业务模块"]
    end

    %% ========== 样式 ==========
    classDef question fill:#FFF3E0,stroke:#FF9800,stroke-width:2px,color:#333
    classDef action fill:#E3F2FD,stroke:#2196F3,stroke-width:1px,color:#333
    classDef restart fill:#E8F5E9,stroke:#4CAF50,stroke-width:1px,color:#333
    classDef warn fill:#FFF9C4,stroke:#FBC02D,stroke-width:1px,color:#333
    classDef shortcut fill:#F3E5F5,stroke:#9C27B0,stroke-width:2px,color:#333

    class Q1,Q_FE,Q_BE,DB1_1 question
    class FE1_1,FE1_2,FE2_1,FE2_2,FE2_3,BE1_1,BE2_1,BE2_2,BE2_3,BE2_4,BE3_1,BE4_1,BE4_2,BE5_1,BE6_1,DB1_NEW,DB1_MOD action
    class FE_RESTART,FE_RESTART_ALL,BE1_RESTART,BE2_RESTART,BE3_RESTART,BE4_RESTART,BE5_RESTART,BE6_RESTART restart
    class C_YES warn
    class C1,C2,C_NO shortcut
