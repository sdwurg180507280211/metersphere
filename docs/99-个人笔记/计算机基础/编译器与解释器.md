flowchart TB
    s11[高级语言源程序<br/>.c / .cpp / .java / .py]
    s11 --> s13[编译执行核心流程]
    s11 --> s14[编译执行核心流程]

    %% ========== 编译执行完整流程 ==========
    subgraph compile[编译执行核心流程]
        direction TB
        预处理[① 预处理<br/>──────────────<br/>处理 # 开头指令<br/>展开头文件 / 替换宏 / 条件编译<br/>纯文本替换，不做语法检查]
        编译核心[② 编译 — 编译器核心]
        汇编[③ 汇编 — 汇编器<br/>──────────────<br/>汇编指令 → 二进制机器码<br/>生成目标文件 .o / .obj<br/>目标文件不可直接运行]
        链接[④ 链接 — 链接器<br/>──────────────<br/>符号解析 + 地址重定位<br/>合并多个目标文件 + 库文件<br/>生成最终可执行程序]
        可执行文件[可执行文件 .exe / ELF]
        加载内存[操作系统加载到内存]
        执行机器码[CPU 直接执行机器码<br/>──────────────<br/>只有机器码能被 CPU 直接执行<br/>汇编语言、高级语言都不行]
        预处理 --> 编译核心 --> 汇编 --> 链接 --> 可执行文件 --> 加载内存 --> 执行机器码
    end
    s13 --> 预处理

    %% ========== 编译器内部6阶段 ==========
    subgraph stages[1. 词法分析<br/>──────────────<br/>字符流 → Token 流<br/>工具：有限自动机 DFA / NFA<br/>识别关键字、标识符、常量、运算符<br/>可检测错误：非法字符 / 非法符号]
        direction TB
        词法分析[1. 词法分析<br/>──────────────<br/>字符流 → Token 流<br/>工具：有限自动机 DFA / NFA<br/>识别关键字、标识符、常量、运算符<br/>可检测错误：非法字符 / 非法符号]
        语法分析[2. 语法分析<br/>──────────────<br/>Token 流 → 语法树 AST<br/>工具：上下文无关文法 CFG<br/>递归下降 / LR 分析<br/>可检测错误：结构错误<br/>缺分号、括号不匹配]
        语义分析[3. 语义分析<br/>──────────────<br/>语法制导翻译<br/>静态语义检查：类型 / 作用域 / 一致性<br/>填写符号表<br/>可检测错误：类型不匹配 / 变量未声明 / 重复声明]
        中间代码生成[4. 中间代码生成<br/>──────────────<br/>AST → 中间代码<br/>形式：三地址码 / 四元式 / 逆波兰 / DAG<br/>便于优化 — 与机器无关<br/>便于移植 — 只改后端]
        代码优化[5. 代码优化<br/>──────────────<br/>等价变换，提升效率<br/>局部优化：常量折叠、公共子表达式删除<br/>循环优化：不变量外提、强度削弱<br/>全局优化：跨基本块<br/>不能改变程序语义]
        目标代码生成[6. 目标代码生成<br/>──────────────<br/>中间代码 → 目标代码<br/>输出：绝对机器码 / 可重定位机器码 / 汇编<br/>涉及：寄存器分配、指令选择、指令调度]
        词法分析 --> 语法分析 --> 语义分析 --> 中间代码生成 --> 代码优化 --> 目标代码生成
    end
    编译核心 --> 词法分析

    %% 贯穿全程的辅助模块
    符号表管理[符号表管理 — 贯穿全程] -.- 词法分析b
    符号表管理 -.- 语法分析b
    符号表管理 -.- 语义分析b
    符号表管理 -.- 中间代码生成b
    符号表管理 -.- 代码优化b
    符号表管理 -.- 目标代码生成b
    错误处理[错误处理 — 贯穿全程] -.- 词法分析a
    错误处理 -.- 语法分析a
    错误处理 -.- 语义分析a
    错误处理 -.- 中间代码生成a
    错误处理 -.- 代码优化a
    错误处理 -.- 目标代码生成a

    %% ========== 前端与后端 ==========
    subgraph fb[前端与后端]
        direction LR
        前端[前端 — 词法 + 语法 + 语义<br/>──────────────<br/>依赖源语言<br/>不依赖目标机器<br/>作用：理解源程序]
        后端[后端 — 中间代码 + 优化 + 目标代码<br/>──────────────<br/>不依赖源语言<br/>依赖目标机器<br/>作用：生成目标代码]
        前端 -.-> 后端
    end

    %% ========== 解释执行完整流程 ==========
    subgraph interpret[解释执行核心流程]
        direction TB
        传统解释[传统直接解释执行<br/>──────────────<br/>解释器逐行翻译<br/>边翻译边执行<br/>实现简单，效率最低<br/>例：早期 BASIC、Shell]
        现代解释[现代字节码解释执行]
        字节码生成[前端编译为字节码<br/>.pyc / .class]
        虚拟机执行[虚拟机解释执行字节码<br/>──────────────<br/>一次编写，到处运行<br/>跨平台性极强]
        即时编译[JIT 即时编译 — 热点代码<br/>──────────────<br/>现代主流语言均采用<br/>兼顾跨平台性和执行效率]
        缓存复用[编译为本地机器码并缓存复用]
        传统解释 --> 字节码生成
        现代解释 --> 字节码生成 --> 虚拟机执行 --> 即时编译 --> 缓存复用
    end
    s14 --> 传统解释
    s14 --> 现代解释

    %% ========== 核心对比 ==========
    subgraph compare[两大执行方式核心对比]
        direction LR
        编译执行[编译执行<br/>──────────────<br/>先翻译，后执行<br/>生成独立可执行文件<br/>运行效率高<br/>可移植性差]
        解释执行[解释执行<br/>──────────────<br/>边翻译，边执行<br/>不生成独立文件<br/>运行效率低<br/>可移植性好]
    end

    %% ========== 文法分类 ==========
    subgraph grammar[Chomsky 文法体系]
        direction TB
        零型文法[0型 — 短语结构文法<br/>无限制产生式 → 图灵机]
        一型文法[1型 — 上下文有关文法<br/>αAβ→αγβ → 线性有界自动机]
        二型文法[2型 — 上下文无关文法 — 考点<br/>A→γ → 下推自动机<br/>用于：程序语言的语法分析]
        三型文法[3型 — 正则文法/正规文法 — 考点<br/>A→aB / A→a → 有限自动机<br/>用于：词法分析]
        零型文法 --> 一型文法 --> 二型文法 --> 三型文法
    end

    %% ========== 中间代码表示 ==========
    subgraph intermediate[中间代码表示 — 常考]
        direction TB
        三地址码[三地址码<br/>──────────<br/>x = y op z<br/>t1 = b * c<br/>t2 = b * d<br/>t3 = t1 + t2<br/>a = t3]
        四元式["四元式<br/>──────────<br/>(op, arg1, arg2, result)<br/>(*, b, c, t1)<br/>(*, b, d, t2)<br/>(+, t1, t2, t3)<br/>(=, t3, _, a)"]
        逆波兰["逆波兰 — 后缀式<br/>──────────<br/>运算符在操作数之后<br/>a+b → a b +<br/>a+b*c → a b c * +<br/>(a+b)*c → a b + c *"]
    end

    %% ========== 有限自动机 ==========
    subgraph automata[有限自动机与正规式]
        direction TB
        DFA[DFA — 确定有限自动机<br/>每个状态每个输入只有一条转换<br/>无 ε 转换]
        NFA[NFA — 非确定有限自动机<br/>可有多条转换 / ε 转移]
        转换链[转换链<br/>──────────<br/>正规式 → Thompson构造 → NFA<br/>NFA → 子集构造 → DFA<br/>DFA → Hopcroft → 最小化DFA]
        DFA --- NFA --> 转换链
    end

    %% ========== 语法制导翻译 ==========
    subgraph sdt[语法制导翻译]
        direction TB
        综合属性[综合属性<br/>──────────<br/>自底向上：子节点→父节点<br/>归约时计算<br/>终结符只有综合属性]
        继承属性[继承属性<br/>──────────<br/>自顶向下：父节点/左兄→子节点<br/>推导时计算]
        S属性文法[S-属性文法：仅综合属性<br/>适合自底向上分析]
        L属性文法[L-属性文法：综合+继承<br/>继承属性仅从左兄/父继承<br/>适合自顶向下分析]
        综合属性 --> S属性文法
        继承属性 --> L属性文法
    end

    %% ========== 常见题型 ==========
    subgraph exams[常见考点题型]
        direction TB
        题型一[题型1：判断编译阶段<br/>──────────<br/>词法 → 非法字符<br/>语法 → 结构错误<br/>语义 → 类型/声明错误]
        题型二[题型2：中间代码转换<br/>──────────<br/>给定表达式写出<br/>三地址码 / 四元式 / 逆波兰]
        题型三[题型3：文法推导与语法树<br/>──────────<br/>画语法树<br/>最左推导 / 最右推导]
        题型四[题型4：有限自动机<br/>──────────<br/>正规式 → NFA / DFA<br/>DFA 最小化]
        题型五[题型5：编译器 vs 解释器<br/>──────────<br/>区别 / 优缺点<br/>混合方式 — JVM]
    end